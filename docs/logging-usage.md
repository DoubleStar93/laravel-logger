## Logging usage (single source of truth)

Questo file era nato come guida “all-in-one”, ma nel package ora teniamo le informazioni in un solo posto per evitare duplicazioni.

Usa questi riferimenti:

- **Uso (typed + defer default)**: `../USAGE.md`
- **Variabili `.env` (incl. payload Kafka e `LOG_STACK`)**: `../env.example`
- **Canali inseriti automaticamente**: `../stubs/logging-channels.stub`
- **Docker OpenSearch**: `../docker/opensearch/README.md`
- **Docker Kafka (+ REST Proxy)**: `../docker/kafka/README.md`

Questo file resta come indice/redirect.

